version: "3.9"

services:
  app:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
    image: cipherrelay-php:latest
    container_name: cipherrelay-app
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
    environment:
      APP_ENV: local
      PHP_MEMORY_LIMIT: 512M
    depends_on:
      - mysql
      - redis
    networks:
      - cipherrelay
    restart: unless-stopped

  web:
    image: nginx:1.27-alpine
    container_name: cipherrelay-web
    ports:
      - "8020:80"
    volumes:
      - ./:/var/www/html:ro
      - ./docker/nginx/conf.d/app.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - cipherrelay
    restart: unless-stopped

  queue:
    image: cipherrelay-php:latest
    container_name: cipherrelay-queue
    working_dir: /var/www/html
    command: sh -lc "php artisan queue:work --verbose --tries=3 --timeout=90"
    volumes:
      - ./:/var/www/html
    depends_on:
      - mysql
      - redis
    networks:
      - cipherrelay
    restart: unless-stopped

  scheduler:
    image: cipherrelay-php:latest
    container_name: cipherrelay-scheduler
    working_dir: /var/www/html
    command: sh -lc "php artisan schedule:work"
    volumes:
      - ./:/var/www/html
    depends_on:
      - mysql
      - redis
    networks:
      - cipherrelay
    restart: unless-stopped

  mysql:
    image: mysql:8.0
    container_name: cipherrelay-mysql
    command: --default-authentication-plugin=mysql_native_password
    environment:
      MYSQL_DATABASE: laravel
      MYSQL_USER: laravel
      MYSQL_PASSWORD: laravel
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "33060:3306"
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - cipherrelay
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: cipherrelay-redis
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - cipherrelay
    restart: unless-stopped

volumes:
  db_data:
  redis_data:

networks:
  cipherrelay:
    driver: bridge
